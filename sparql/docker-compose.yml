version: '3.8'

services:
  fuseki:
    image: stain/jena-fuseki
    ports:
      - "3030:3030"
    # volumes:
    #   - ../data/rdf:/fuseki/databases
    environment:
      - FUSEKI_DATASET_1=/ds
      - ADMIN_PASSWORD=admin
    restart: always
